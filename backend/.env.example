# ===========================================
# DATABASE
# ===========================================
# Pour SQLite (développement)
DATABASE_URL="file:./dev.db"

# Pour PostgreSQL (production)
# DATABASE_URL="postgresql://user:password@host:5432/database_name?schema=public"

# ===========================================
# SERVER
# ===========================================
PORT=3000
NODE_ENV=development

# En production, définir : NODE_ENV=production

# ===========================================
# JWT SECRET
# ===========================================
# ⚠️ IMPORTANT : Changez cette valeur en production !
# Générez une clé aléatoire forte (ex: openssl rand -base64 32)
JWT_SECRET="votre_secret_ultra_securise_changez_moi_en_production_123456"

# ===========================================
# EMAIL (optionnel - pour envoi emails)
# ===========================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=votre.email@gmail.com
EMAIL_PASSWORD=votre_mot_de_passe_app
EMAIL_CONTACT=contact@votre-sci.fr
COMPANY_NAME=Votre SCI

# ===========================================
# FRONTEND URL (pour CORS et redirections)
# ===========================================
# En développement
FRONTEND_URL=http://localhost:5173

# En production, remplacer par :
# FRONTEND_URL=https://votre-domaine.com

# ===========================================
# DÉPLOIEMENT
# ===========================================
# Pour héberger sur un serveur distant :
#
# 1. BACKEND :
#    - Définir DATABASE_URL vers PostgreSQL
#    - Changer JWT_SECRET
#    - Définir NODE_ENV=production
#    - Définir PORT (ou laisser automatique)
#    - Configurer FRONTEND_URL
#
# 2. FRONTEND :
#    - Définir VITE_API_URL vers l'URL de production du backend
#
# Exemples de configurations production :
#
# Backend hébergé sur Railway/Render :
# DATABASE_URL=postgresql://user:pass@host.railway.app:5432/railway
# JWT_SECRET=<généré avec openssl rand -base64 32>
# NODE_ENV=production
# FRONTEND_URL=https://votre-app.vercel.app
#
# Frontend hébergé sur Vercel/Netlify :
# VITE_API_URL=https://votre-api.railway.app/api
